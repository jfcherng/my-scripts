[user]
	name = Jack Cherng
	email = jfcherng@gmail.com
[color]
	ui = true
[alias]
	a = add
	bl = blame
	br = branch -vv
	chp = cherry-pick
	ci = commit -s
	cl = clean
	co = checkout
	contributors = shortlog --summary --numbered
	d = diff --patch-with-stat
	dt = difftool
	fp = format-patch
	ft = fetch --tags --force --prune
	grp = grep -n --color -E
	l = log --graph --date=short --color --pretty=format:'%C(yellow bold)%h%C(auto)%d%Creset - %s %C(green bold)[%an]%Creset %C(blue bold)(%ad, %cr)%Creset'
	lg = log --graph --date=short --color --pretty=format:'%C(yellow bold)%h%Creset %G?%C(auto)%d%Creset - %s %C(green bold)[%an]%Creset %C(blue bold)(%ad, %cr)%Creset'
	m = merge
	mt = mergetool
	parent = "! git show-branch | grep '*' | grep -v \"master\" | head -n1 | sed 's/.*\\[\\(.*\\)\\].*/\\1/' | sed 's/[\\^~].*//' #"
	pl = pull --rebase
	pull = pull --rebase
	pushf = push --force-with-lease
	rb = rebase
	rs = reset
	sh = show --patch-with-stat
	shrink = ! git reflog expire --expire=now --all && git gc --aggressive --prune=now
	sm = submodule
	st = status --show-stash
	sync = ! git fetch --tags --force --prune --all && git reset --hard @{upstream}
	wt = worktree
[core]
	autocrlf = false
	editor = vim
	quotepath = false
	ignorecase = false
	safecrlf = false
	filemode = true
	pager = diff-so-fancy | less --tabs=4 -RFX
	hooksPath = .githooks
	attributesfile = ~/.gitattributes
[push]
	default = simple
[i18n]
	commitencoding = utf-8
[gui]
	encoding = utf-8
[diff]
	tool = bc
	algorithm = patience
[difftool]
	bc3 = trustExitCode
	prompt = false
[merge]
	tool = bc
[mergetool]
	bc3 = trustExitCode
	prompt = false
[difftool "bc"]
	path = bcompare
[mergetool "bc"]
	path = bcompare
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = 227
	frag = magenta bold
	commit = 227 bold
	old = red bold
	new = green bold
	whitespace = red reverse
[diff "ppt"]
	textconv = catppt
[diff "xls"]
	textconv = xls2txt
[diff "odt"]
	textconv = odt2txt
[diff "doc"]
	textconv = catdoc
[diff "pandoc"]
	textconv = pandoc --to=markdown
[diff "catdoc"]
	textconv = catdoc
[diff "catppt"]
	textconv = catppt
[diff "xls2txt"]
	textconv = xls2txt
[diff "odt2txt"]
	textconv = odt2txt
[diff "xdoc2txt"]
	textconv = xdoc2txt -8
